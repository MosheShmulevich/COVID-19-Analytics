import Status
import Test


if Status == 1:

    class Patient(Test):  # the class takes all the info from Test class , and adds status to the patient.
        def __init__(self, status):
            self.status = Status.status_change(self)













